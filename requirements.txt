# Lokal OCR-pipeline for namngiving av NB-fotofiler.
# Krev Python 3.9-3.13 (RapidOCR/onnxruntime har ikkje wheels for 3.14 enno).
rapidocr>=3.9.0
onnxruntime>=1.20.0
pillow>=10.0.0
numpy>=1.24.0

# Web-app (lokal browser-UI)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.5.0

# GPU (valfritt): installer torch med CUDA separat, t.d.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
